# Helm deployment

Reserved for the production control plane, Kubernetes Agent Sandbox controller integration, router, RBAC, NetworkPolicy, storage, and observability.

Do not copy mikan's former combined chart directly. The platform chart must not deploy mikan and must expose only the control-plane contract.
